# LEDGER — zgłoszenia beta-testera VEGAN (PDF 2026-06-11)

Źródło: `Proponowane zmiany i modyfikacje (Trap Simulator) (1).pdf` (12 stron).
Netlify live: https://trapsimulator.netlify.app/ — **każdy błąd testować w OBU scenariuszach** (file:// lokalnie + https Netlify).
Patch notes: KAŻDY wpis → `cats:['beta-tester','Vegan',typ], reporter:'Vegan'`.
Tempo: **jeden błąd na turę, z potwierdzeniem usera.**

Status: ⬜ TODO · 🔄 W TOKU · ✅ DONE · ⏸ DEFER (decyzja usera)

### ⚠️ KLUCZOWE ODKRYCIE (2026-06-11) — minifikacja Netlify = źródło klasy „tylko Netlify"
`deploy/netlify.toml` dokumentuje: minifikacja inline-JS (~5MB) psuła logikę → objawy *„brak wejścia do budynku"* + *„fałszywe 'już dziś zrobione'"* = DOKŁADNIE objawy Vegana (B2/B5/B6/B7). Fix `skip_processing=true` dodany 6 cz. Żywy Netlify = **bajt-w-bajt identyczny z lokalnym** (5302910 B, v2.1.818), NIE zminifikowany — zweryfikowane `curl`+`cmp`.
**STRATEGIA: test na LOKALNYM `file://` = źródło prawdy.** Repro lokalnie → prawdziwy bug → fix. Brak repro lokalnie+Netlify → był artefakt minifikacji/cache (Vegan: hard-refresh Ctrl+Shift+R). „Na obu" = prawdziwy kod; „tylko Netlify" = prawdopodobnie artefakt.

---

## SEKCJA A — BŁĘDY (priorytet, jeden na turę)

- ✅ **B1. Drzewko Scena 5 vs 8 — NAPRAWIONE (v2.1.819, commit fd5d83c).** Root cause: `_buildGraph` L32971 hardcodował `{length:5}` kropek poziomu w węźle drzewka (Scena ma 8 nodes). Half-fix v2.1.231 naprawił panel szczegółów + buyRaperST (nodes.length) ale POMINĄŁ kropki węzła. Fix: `{length:def.nodes.length}` (110px mieści się w NW=122px). Runtime `dbg_b1_scena_dots.mjs`: LOCAL scena 6/8 PASS, NETLIFY(stary) scena 5/5 = dowód że bug był realny. Round 2: buyRaperST+details już OK, brak innych renderów raperST, getScenaEnergyBonus używa realnego lvl. To był prawdziwy bug kodu (występował też lokalnie/„na obu"). Netlify dostanie fix po redeploy (deploy/ zsynchronizowane).
- ✅ **B2. Sociale (Instagram) — RESOLVED (artefakt minifikacji/cache Netlify, NIE bug kodu).** Kod resetu poprawny, repro DZIAŁA na obu środowiskach v2.1.818. Vegan widział na starym zminifikowanym buildzie. Akcja: hard-refresh Netlify. Bez zmiany kodu. (szczegóły niżej)
- ⬜ ~~**B2. Sociale (Instagram) — nie można publikować kolejnego dnia.**~~ Po 1. opublikowanym poście (dowolny typ) następnego dnia w TEJ SAMEJ sesji publikacja kolejnego posta (zakulisowy/promo/sponsor) jest niemożliwa — wymaga resetu sesji. Podejrzenie: flaga dzienna nie resetuje się przy zmianie dnia. [oba]
- ✅ **B3. Sequencer poradnik per gatunek — NAPRAWIONE (v2.1.821, commit 92b3269).** Root cause: standalone Beatmaker (createBeat→sequencer, „Stwórz bit") NIE miał wyboru gatunku → `_initSequencer` czytał `G.currentTrack.genre`=zawsze default 'trap' → podpowiedzi/BPM zawsze trapowe. Wybór istniał tylko w pełnym DAW (`daw-genre` select). GENRES patterns RÓŻNIĄ się per gatunek (zweryfikowane). Fix: dropdown gatunku na ekranie wyboru trybu (`_showModeChoice`) ustawia `G.currentTrack.genre` (reużyte GENRE_KEYS/GENRES) + defensywny init currentTrack. Runtime `_b3_verify.mjs` PASS: dropdown steruje genre, hinty trap≠drill≠rnb. Round 2: DAW path nietknięty (oba piszą ten sam field), init nie nadpisuje istniejącego tracka.
- ✅ **B4. Sequencer brak dźwięku — NIE repro (kod działa na obu env).** Runtime `dbg_b4_seq_audio.mjs`: playStep2 tworzy oscylatory (3) dla aktywnych komórek, ctx='running', sfxVol=1, _sfxMasterGain=1 — na LOCAL i NETLIFY. `_b4_gesture_min.mjs`: po realnym geście getAudio() wznawia ctx→'running' bez flagi autoplay (oba env). Kod: getAudio (L14767) resume-on-suspended + _sfxMasterGain→compressor→destination poprawny; playStep2 (L27548) oscylator per active cell. Generowanie dźwięku w pełni sprawne. Vegan = artefakt zminifikowanego Netlify (netlify.toml objaw) lub jego środowisko (wyciszony SFX/audio device). Bez zmiany kodu; hard-refresh Netlify. WZORZEC: B1/B5 (dane/mapa) = realne+naprawione; B2/B4 (flaga/audio, minifikacja-sensitive) = nie repro.
- ✅ **B5. Obiekty niewidoczne na mapie — NAPRAWIONE (v2.1.820, commit d8b4e85).** Root cause: shelter/zoo_shop/vet_clinic BRAKOWAŁY w `MAP_LANDMARKS` (L47719) → brak etykiet na pełnej mapie miasta. Przy przenosinach do clean zone wx 4280/4440/4600 (v2.1.537) nie naniesiono ich na mapę. Fix: +3 wpisy (centra 4345/4505/4665, wy 749, cat shop/shop/health). Runtime `_b5_maplabels.mjs`: 3 etykiety renderują się na mapie (DOM), MAP_LANDMARKS 45→48. Render in-world działał (draw cases L3565-3567). Round 2: NPC-minimapa LOCS celowo bez nich (NPC-only), współrzędne w granicach, brak overlapu.
- ✅ **B6. Wejście do Schroniska — NIE bug kodu (działa lokalnie, bramkowane).** Runtime `dbg_b5b6_v2.mjs`: schronisko OTWIERA się gdy masz mieszkanie Basic+ (bramka by design: „pies nie zmieści się w traphouse/studio"); zoo/vet otwierają się gdy masz psa. Bez mieszkania → czytelny komunikat „kup mieszkanie". To gate, nie awaria. Vegan „nie da się wejść" = (a) brak mieszkania, (b) nie mógł znaleźć (B5, teraz fix), lub (c) minifikacja Netlify (netlify.toml: „brak wejścia do budynku"). Hint dog_intro (L47556) komunikuje wymóg mieszkania na lvl3+. DECYZJA USERA (2026-06-11): ZOSTAWIAMY bramkę bez zmian (wymóg mieszkania sensowny, komunikowany hintem, po B5 budynki znajdowalne). B6 zamknięte bez zmiany kodu.
- ✅ **B7. Dzielnice — 1 realny bug naprawiony (v2.1.822, commit 68f0d23), reszta OK.** Audyt `dbg_b7_districts.mjs` WSZYSTKICH 21 miejsc: 20/21 działało lokalnie (modal/changeRoom/komunikat-bramki), 0 trafiało w „Content w przygotowaniu". Komentarz L35118 („placeholdery") nieaktualny — wszystkie miejsca mają handlery w `_enterDistrictPlace`. Jedyna realnie martwa: **coast_marina** — `_startMarinaChoice` renderowało treść do mg-job-content ale NIE dodawało `.on` do #mg-job → panel ukryty = cicha awaria. Fix: `_mg.classList.add('on')`. Po fixie 21/21 OK. Vegan „reszta niefunkcjonalna" = zminifikowany Netlify (jak B2/B4) + bramki (fame/kasa/Trap-unlock). Round 2: test = audyt analogów 21 miejsc.

---

## SEKCJA B — ZMIANY/WDROŻENIA (balans + design, po błędach; część DUŻA → decyzja usera)

- ⏸ Z1. Pasek zadaniowy — ukryć z ciągłego widoku (toggle/ikona przy krawędzi); pojawia się przy ukończeniu czynności; odhaczanie zadań poza kolejnością + nagrody.
- ✅ Z2. Ekipa exploit — NAPRAWIONE w 2 etapach (v823 + **v845 dokończenie**). **v823** (commit d6d86c3): TYLKO Negocjacja Łukasza (manager_jr cd:7, cost:1000) + rekrutacja ×3. **ALE pozostałe 4 akcje dalej darmowe+codzienne** — user (2026-06-12) wytknął: „za bonusy/przychód płaci się i to sporo, zero spamu". **v845 (commit 75331d9):** akcja dj „Sprzedaj bit +500 zł za darmo" **USUNIĘTA** (nonsens click-money; Olek zostaje z pasywnym +15% bity); **[v846 koszty flat 5000/6000/5000 → v847 PRZEBUDOWA na „% przychodu jak Łukasz"]** Analiza (user „sprawdź czy kwoty i to co dają ma sens, koszt trochę < zysk, skalujący z rozwojem"): płaskie efekty (+2000 streamów `track.streams`=vanity bo royalty z growth nie streams; +2000 followers≈40 zł/sponsor; +10 jakości≈160 zł/release) **warte ~0 zł a kosztowały tysiące = ZAWSZE głęboko ujemne**. Tylko Negocjacja Łukasza była OK (wzorzec). **v847 model:** akcje GENERUJĄ przychód skalujący z zasięgiem (`_crewGrossYT=(followers+listeners)×0.01`, `_crewGrossHype=followers×0.012`, `_crewGrossMaster=followers×0.008`, soft-cap asymptota ~100k), koszt=`_crewFee`=max(500, 60% grossu) → net +40% dla rozwiniętych, lekki minus early (floor). Koszt-fn i exec liczą ten sam gross (deterministyczny=spójne). Bartek=kampania YT, Seba=viral push(+obserwujący), Mariusz=remaster(+zachowany buff +10 jakości). Runtime `dbg_crew_income_v847.mjs` **16/16 PASS** (EARLY 10k: −350/−380/−420; MID 100k: +600/+480/+300; LATE 1M: +6000/+4800/+3200; ENDGAME 10M: +33k/+31k/+27k). **LEKCJA: „wysoki koszt" bez „wysokiej wartości" = ripoff — koszt MUSI być ułamkiem realnego (policzonego!) zysku. + Test money-delta był zanieczyszczony przez addFans→checkAchievements (nagrody za osiągnięcia przy napompowanych statach) → mierz model bezpośrednio z funkcji gross/cost.** **manager_jr Negocjacja: koszt 1000→15% szacowanego przychodu z koncertu** (`G._concertTopEst` liczony przy otwarciu Sali = single-source estymatora, floor 200, cd 7) → proporcjonalny do zysku. `_crewAction` obsługuje koszt-funkcję; panel pokazuje koszt na przycisku + blokuje przy braku kasy. Pasywne bonusy opłacane wypłatami (400/os×(1+followers/10000), L22259). Runtime `dbg_crew_v845.mjs` **16/16 PASS**. **LEKCJA: „naprawione" w 1 miejscu ≠ cały system — Z2 v823 ruszył tylko 1 z 5 akcji; user złapał resztę. Grep WSZYSTKICH analogów (reguła #6).**
- ⏸ Z3. Opłaty utrzymania labelu — progi opłat przy zakładaniu + rekrutacji pracowników (nie artystów).
- ✅ Z4. Koszty utrzymania ↑ — ZROBIONE (v2.1.826 + v2.1.830). Czynsz CAŁA drabina (APT_RENT): traphouse 50→**1000**, basic 250→**2000**, premium 600→**4500**, luxury 1800→8000, duplex 2800→15000, mansion 5000→35000, villa 12000→90000. (v830→v831: user 2× dopisał że tanie mieszkania mają za niski czynsz, traphouse min 1000/tydz; spłacalne wczesną grą, 7 dni terminu, eksmisja +5 dni.) Ekipa baza 150→400/os. Admin wynajmu 50%→20% (wynajem innym pozostaje opłacalny, reguła #7). Beat-pack/single dostały koszt energii (Z6, v2.1.825). Część pakietu Z6+Z4. POZOSTAJE z tej kategorii: Z5 (serwis auta), Z13 (szpital) jako osobne pozycje.
- ✅ Z5. Serwis auta — REALNE UTRZYMANIE (v2.1.842→**843**, commity 38236f8+3c94bc1). v842 zrobił koszt proporcjonalny (1%/floor60) ALE user: „nie chodziło o potanienie Malucha, 300 to za mało" + „sprawdź jak często się płaci". **Weryfikacja częstotliwości (kluczowa):** zużycie było −2%/dzień gry, dzień=6 min realnych (`_BASE_TIME_SCALE 4.0`) → pełne 100→0 = **5h gry**, konsekwencja TYLKO utrata +20% prędkości mapy (`×(1+0.2×cond/100)` L12410, nigdy poniżej bazy) → serwis opcjonalny i super-rzadki. **v843→844 (combo 1+3 + 5%/floor500, decyzja usera):** (1) czas −2→**−4%/dzień** (L22375, v844: user dostroił 5→4); (2) **zużycie od JAZDY** −1% co **4500 px** przejechane (`updatePlayer` po boost, frame-rate independent per world-distance, `G._carWearDist`); (3) **HUD** „🔧 X%" nad autem podczas jazdy (`drawCar` ~L11450, zielony/pomarańcz/czerwony); (4) koszt `_carServiceCost` 1%→**5%**, floor 60→**500**: Maluch 500/BMW 1250/Audi 4000/Lambo 15000/Bugatti 60000. Save-safe (`||0`, price fallback 25000). Częstotliwość teraz: niejeżdżący ~2h, średni ~1h, hardcore ~30 min. Runtime `dbg_car_wear_v843.mjs` **11/11 PASS** (czas −5, jazda −1%/4500px, koszty wg tabeli, HUD bez błędu, zero błędów). Patch note v842 skonsolidowany w v843. LEKCJA: przy „balans kosztu" ZAWSZE najpierw policzyć CZĘSTOTLIWOŚĆ płacenia (decay rate × długość dnia realnego × konsekwencja) — koszt bez częstotliwości = brak sinka.
- ✅ Z6. Zmniejszenie zarobków — PEŁNY PAKIET ZROBIONY (v2.1.824–834). **Aktywne:** koncert asymptota 180k→95k→**60k** (knee 40k; top ~180k→~59k), beat-pack/single koszt energii (−10/−3 = koniec spamu AFK). **Pasywne:** trap terytoria −30%, artyści wytwórni −25%, **trap dealerzy auto-sell −30%** (v832), **tantiemy Spotify/Tidal/AdSense/Radio −30%** (v833, ×0.7), **streamy donejty −25% + sponsoring IG/Twitter −25%** (v834). Monte Carlo (oddany gracz late-game, koncert-heavy): netto ~475k→~185k/dzień (−61%), 1 mln ~2,1→~5,4 dnia; pasywni gracze (dealerzy/tantiemy) dodatkowo −30%. **ZOSTAWIONE celowo (nie karać wysiłku, reguła #11):** turniej gamera (już ×0.5+cap25k+1/dzień), kontrakty studyjne (praca), zaliczki labelu (jednorazowe), kasyno (RTP ~3% celowo), pasyw AFK panel (decyzja 2026-06-03 „panel=prawda"). Drug-price popyt/podaż = osobna pozycja Z14.
  - **DOCISK PROPORCJI v2.1.840 (commit f9e779f, wybór usera „ostry C"):** monotoniczna kara spam koncertów przez JEDEN helper `_concertSpamPenalty(n)` (single-source: estymator+realna wypłata+komunikaty+UI). Schedule: 1.=100% (pełny, brak odbierania), 2.=50%, 3.=33%, 4.=25%, 5.+=20% (4./5. defensywny fallback — hard cap 3/dzień bez zmian). Naprawia: **inwersję** (stary 3.=75% płacił WIĘCEJ niż 2.=50% → premiował spam), **desync estymatora** (pokazywał 4.+ jako 100%/„BLOKADA"), mylący komunikat bookingu „Payout 75%". Festiwale/trasy zwolnione. Runtime: 3 koncerty/dzień **133k→108.5k**, udział koncertów **64%→59.1%**. LEKCJA: był HARD CAP 3 koncerty/dzień (Amper L34566) — tiery 4./5. nieosiągalne, panel pokazuje tylko realne 3. 1. koncert nietknięty = brak „odbierania" głównej aktywności.
- ⏸ Z7. Livestream YT — donate/sub/viewsy progresywne wg etapu sławy (start = małe liczby).
- ⏸ Z8. 2–3× wydłużenie doby w grze.
- ⏸ Z9. Ofertowanie koncertów — gating: brak dziesiątek ofert na starcie; wymóg ~2-3 nagranych kawałków; małe lokale za kilkaset zł → wzrost wraz z rozpoznawalnością.
- ⏸ Z10. Więcej poziomów relacji z NPC + więcej NPC (SMS, telefon, small-talk przy spotkaniu).
- ✅ Z11. Sytuacyjne nieprzyjęcie numeru/albumu/beatu — ZROBIONE (v2.1.852, commit 50ec18f). Helper `_releaseReception(quality)` (top-level, przed sellBeat): FLOP (szansa `max(0.06, 0.28−q/100×0.20)` → q20=24%/q50=18%/q90=10%; mult 0.30-0.50) LUB HIT (8%, mult 1.5-2.1) LUB normal. Transparentne (label „📉 Numer nie chwycił"/„🚀 Numer ZASKOCZYŁ"). Single-source w 5 ścieżkach: single Spotify (gwarantowany hit z Teksty NIE flopuje — override), single SoundCloud, album rapera (pay+spListeners), album beatmakera, beat (gain). **Dema CELOWO pominięte** (nie karać early-game). Runtime `dbg_release_reception_v852.mjs` **11/11 PASS**. Save-safe. Reguła #11: lepsza jakość = mniejsze ryzyko flopu (realizm, nie hidden nerf).
- ⏸ Z12. Nastrój wg pogody — ławka w parku przy deszczu/śniegu/burzy POGARSZA nastrój.
- ⏸ Z13. Szpital — leczenie nastroju (Terapia/Psychoterapia) 2–3× drożej.
- ✅ Z14. Ceny narkotyków wg popyt/podaż — ZROBIONE (v2.1.839, commit c79d974). Każdy typ (joint/shard/powder/mix/lsd/ecstasy) ma `G.trap.market[id]` ~0.6–1.4. **Podaż:** każda sprzedaż spycha cenę typu w dół (asymptota 0.6, gładko per sztuka — reguła #11). **Popyt:** dzienny mean-revert ku 1.0 + losowa fala per typ. Średnia=1.0 → ZERO systemowego nerfu, sama wariancja (dywersyfikuj/czekaj=📈, zalewaj=📉). Czystość partii osobno (_trapQMult). Helpery `_trapMarketEnsure/Mult/SupplyHit/DailyTick`, zastosowane: sprzedaż ręczna+transakcja MG+dealer Kamper+panel telefonu, UI chip 📈/➖/📉, defensive init loadGame (save-safe). IDs zweryfikowane = recipe.id (nie ghost). Runtime: supply→0.6, revert→1.0, średnia 1.0004, monotoniczne. (Implementacja była niezacommitowana z poprzedniej tury — zweryfikowana i zamknięta atomowym commitem.)
- ⏸ Z15. Tajne suplementy Zbyszka (budka kebab) — po zaprzyjaźnieniu sprzedaje suplementy (boost zdrowia/kondycji, drogie).

---

## SEKCJA C — PROPOZYCJE (duże, opcjonalne; tylko po akceptacji usera)

Storyline/typy kariery · ambience SFX · sprzęt produkcji DO KUPIENIA · sejf ścienny · oszustwa NPC · DJ do zespołu · bank (lokaty/oferty/limit) · alternatywne banki · inwestycja w kruszec · klub striptizu · spowolnienie nastroju+nagłe spadki · spowolnienie łaknienia wg aktywności · STATUS ZNIESŁAWIENIA · uzależnienie od substancji · kontrole skarbowe/naloty · odsiadka więzienna · OLX/ogłoszenia · ekwipunek-okienka/plecak · noszenie własnego merchu (drip) · kradzież mienia przez gracza · broń palna · alternatywne storyline NPC + underground NPC · nowe gatunki+manual BPM w sequencerze · nowe lokacje (złomowisko/JYSK/PEPCO-Lumpeks) · nowe prace dorywcze (ulotki/magazyn/koszenie/budowa/mechanik/resell/złom/statysta/fotograf/ochroniarz/event staff) · odzież „zwykła".

---

## DZIENNIK PRACY

### v2.1.849 — freestyle: polskie znaki przez AltGr (2026-06-12, user-flagowane)
User: „u jednych graczy polskie znaki we freestyle działają (łańcuch), u innych nie". ROOT CAUSE: handler `_fsKeydown` (L39463) miał `if(e.key.length===1 && !e.ctrlKey && !e.altKey && !e.metaKey)` — blokował KAŻDY klawisz z wciśniętym Altem. Polskie znaki na układzie **PL-programisty** wpisuje się przez **AltGr**, który na Windows ustawia `altKey=true` (często i `ctrlKey=true`) → znak odrzucany. Niespójność: gracze z AltGr zablokowani; natywny układ PL (znaki bez AltGr) + mobile (ukryty input, `altKey:false`) działali. FIX: akceptuj każdy pojedynczy znak nie-ASCII (`charCode>127` = polskie litery, zawsze realne wpisanie znaku, nigdy skrót) niezależnie od ctrl/alt; ASCII tylko bez modyfikatorów (skróty pomijane). Analogi (reguła #6): L1692 dev-cheat-seq (tylko `?dev=1`, ASCII) + L1701 keyup-cleanup — niezwiązane, zostają. Inne minigry wpisywania używają natywnych `<input>` (Polish OK). Runtime `dbg_freestyle_polish_v849.mjs` **6/6 PASS** (łańcuch/sława kończą się przez AltGr altKey+ctrlKey true). Commit d84ca8e. LEKCJA: keydown filtrujący znaki MUSI obsłużyć AltGr — nie blokować na samym altKey.


### v2.1.848 — KONCERTY: repertuar + jakość nagrań (2026-06-12, user-flagowane spoza listy Z)
User: (1) SMS Marcina pierwszego dnia nieimmersyjny — wymagać nagranych piosenek; (2) koncerty wymóg nagranych piosenek + sprawdzić zliczanie; (3) jakość nagrań (traphouse/słaby mic vs studio/sprzęt/miks/master/wtyczki) w mnożniku koncertu; (4) transparentność (+/− z jakości na koncertach + tantiemach).
- **Helpery single-source** (przed `renderSalaKoncertowa`): `_recordedSongs` (studioTracks bez `type==='instrumental'` = dema Home Studio + single SC/Spotify), `_recordedSongsCount`, `_avgRecordedQuality`, `_concertQualityMult` (0.70+q/100×0.6, clamp 0.7-1.3; q50=1.0/q20=0.82/q90=1.24).
- **Wymóg piosenek**: `tracksReq` w venues (Mała 2/Klub 4/Arena 8/Coachella 15/Wembley 20/Megastar 25) → `blocked`+lockReason „Nagraj X piosenek (masz Y)"+req na karcie+„🎵 Piosenki: N" w nagłówku.
- **Jakość w mnożniku**: estymator `baseMult` (L33880) + endConcert `earn` (L34962) ×`_concertQualityMult()` — SPÓJNE. Chip „🎚️ Jakość nagrań X%: koncerty ±Y%".
- **Gate ofert**: `songsReq` w LEAD_TYPES (club_local 2/club_warsaw 4/festival_small 8/festival_big 15/radio 2-5/wedding 3/private 6; sponsor IG=0) + filtr w `_maybeGenerateLead`. Koniec SMS Marcina day-1 bez nagrań.
- **Tantiemy transparentne**: komunikat wydania singla Spotify+SC „Jakość X% → Y streamów, +Z zł tantiem (wyższa jakość = więcej)".
- **Hint** `record_first` (scena ale <2 piosenek) + concert hint uzupełniony o jakość.
- Runtime `dbg_concert_tracks_v848.mjs` **15/15 PASS** (zliczanie 2 piosenki+instrumental wykluczony, jakość monotoniczna, blokada/odblok, oferty 0→2, estymator 423 vs 656). Save-safe. Commit 580b6f2.

(uzupełniam per błąd: repro przed → root cause → fix → test po → commit → wersja)

### Z6+Z4 — Rebalans ekonomii „multimilioner w kilka dni" (2026-06-11, v2.1.824–829)
Headline Vegana. Decyzja usera: dochód ↓ + koszty ↑ (agresywnie), potem „ciśnij koncert mocniej".
Weryfikacja PRZED (reguła #5): koncert formuła REALNA = knee 60k/asymptota 180k (`60000+excess/(1+excess/120000)` w 2 miejscach: estymator L33798 + endConcert L34858); wpis PATCH_NOTES L52479 = STARY (80k/180k, historyczny — nie ruszany). Beat-pack `rnd(800-2000)+midiQ×5+jakość×6` BEZ energii. Trap terytoria income 375/688/1063/1625. Artyści dailyIncome 80–2200.
Implementacja (6 atomic commitów):
- v2.1.824 koncert: knee 60k→40k, asymptota 180k→95k + endgame venues −33% (Coachella/Wembley/Megastar).
- v2.1.825 beatmaker: sellBeat −3 energii, sellBeatPack −10 energii + guard + UI karty (reguła #3).
- v2.1.826 koszty: APT_RENT górne tiery ×2,5–7,5, ekipa 150→400, admin wynajmu 50%→20% (anti-pułapka, reguła #7).
- v2.1.827 trap: TERRITORY_DEFS income −30% (single-source: UI/tick/tribute czytają t.income).
- v2.1.828 wytwórnia: SIGNABLE_ARTISTS dailyIncome −25% (15 artystów).
- v2.1.829 koncert „mocniej": asymptota 95k→60k (param /55000→/20000, knee 40k). Top koncert ~90k→~59k.
Monte Carlo (node, oddany gracz late-game raw koncert 600k ×3/dzień): netto ~475k→~185k/dzień (−61%), koszty ×4,6, 1M w ~2,1→~5,4 dnia. Krzywa koncertu: monotoniczna, gładka, asymptota — reguła #11 trzymana (node-probe).
Round 2 (reguła #18): koncert sync 2 miejsca ✓; crew DJ action = flat +500 zł, NIE woła sellBeat() (brak bypassu energii) ✓; rent-out po zmianie nadal dodatni (luxury +1900, villa +24000/tydz) ✓; save-safe (brak nowych pól, istniejące bills zachowują amount) ✓. **deploy/ (Netlify, untracked) był v2.1.823+CRLF = zsynchronizowany do v2.1.829 (0 różnic treści).**
DOCISNIĘCIE v832-835 (user „pełny pakiet"): trap dealerzy −30% (`_prices` L17373), tantiemy Spotify/Tidal/AdSense/Radio −30% (×0.7), streamy −25% + sponsoring IG/Twitter −25% (×0.75), **MERCH −30%** (user wytknął pominięcie: `calcDemand` coef 0.0006→0.00042 + HARD_DAILY_CAP 50k→35k + random event). deploy/ re-sync po każdym commicie do v835 (0 różnic).
SWEEP pozostałych pasywnych: (1) **Evolution passive −30% ZROBIONE (v837)** — payout L22755-22772 (supermarket/gamer 1000→700, influencer 700→490, soundeng/beatmaker 900→630, youtuber 1200→840, inwestycje 800→560, kreatywność 1500→1050) + notif + panel L44139 zsynchronizowany. User wybrał nerf (nadpisał decyzję 2026-06-03). (2) **Dochód z wynajmu — ZOSTAWIONY** (decyzja usera, `_rates` L22599 bez zmian). (3) kebab 300/dzień (trywialny, zostaje). (4) bank lokaty = kapitał gracza. (5) staking krypto już znerfiony.
**DODATKOWE wykryte przy pytaniu o „panel=prawda" (v836):** (a) **SoundCloud −30%** (Z6 wymieniał „SC", pominięty w v833) — payout L22558 + panel scDaily. (b) **FIX desync panelu** (reguła #3): panel „Szacowany dzienny przychód" liczył Spotify/Tidal/Radio/YT starymi stawkami → zawyżał ~43% po v833; dodane ×0.7 do estymat panelu. LEKCJA: panel pasywny (L44100+) ma WŁASNE kopie formuł — każdy nerf income trzeba zsynchronizować i tam (panel=prawda = panel MUSI zgadzać się z realną wypłatą).
**ANALIZA PROPORCJI + v838 (user „sprawdź proporcje koncert vs reszta"):** model dziennego dochodu maxed gracza wykazał koncerty = 70% (poprawa z 81%, ale wciąż 2,4× reszta). ZNALEZIONY BUG: kara spam koncertów (50%/75% za 2./3.) aplikowana PRZED kompresją → kompresja ją połykała (2. koncert raw×0.5=300k wciąż ~58k, wskaźnik „50%" KŁAMAŁ). FIX v838: kara PO kompresji (endConcert L34870 + estymator L33810 sync). Efekt: 2. koncert realnie 50%, 3. realnie 75%; koncert/dzień 177k→133k; udział 70%→64%; 1. koncert bez zmian (brak odbierania). Naprawia mylącą UI (reguła #3) + anty-spam + proporcje naraz. node-verified. LEKCJA: kolejność operacji (kara×mnożnik PRZED vs PO kompresji/soft-capie) DRASTYCZNIE zmienia efekt — soft-cap „połyka" mnożniki aplikowane przed nim.

### B2 — Instagram next-day (2026-06-11) — ⚠️ NIE REPRODUKUJE SIĘ na v2.1.818
Krok 0 readiness: OBA środowiska READY (file:// + Netlify), oba **v2.1.818** (Netlify NIE jest starszy).
Runtime testy (oba środowiska):
- `dbg_b2_ig_nextday.mjs` — ścieżka `igPost()` bezpośrednio: post #2 następnego dnia **DZIAŁA** (reset count 1→0 po day++).
- `dbg_b2_composer_path.mjs` — prawdziwa ścieżka UI `_igOpenComposer`→`_igPublishFromComposer`: przyciski WRACAJĄ next-day, post #2 **DZIAŁA**.
Kod zweryfikowany: reset `if(G._igPostDay!==G.day){...count=0}` w 3 miejscach (L18417, L40945, L41108) — poprawny. `togglePhone` (L40552) zawsze renderuje home → tapnięcie IG = świeży render z resetem. Hipotezy stale-render i Netlify-starszy = OBALONE runtime.
WNIOSEK: bug w dosłownym brzmieniu nie repro na 818. Możliwe: (a) Vegan testował starszą wersję (naprawione od tamtej pory), (b) percepcja „nowego dnia" gdy gracz spał ale NIE przekroczył północy (gameMin<1440 → `G.day` się nie zwiększa → limit nie resetuje; dotyczy WSZYSTKICH dziennych aktywności, nie tylko IG). CZEKA NA: dokładne kroki repro od Vegana (typ posta? spał czy czekał? czy reopen apki naprawia czy tylko pełny reload? jaka wersja?).

### v2.1.849b — Vegan w puli freestyle + AltGr fix bez patch note (2026-06-12)
User: (1) zmiana AltGr (poprzedni commit) NIE ma byc w patch notes — wpis usuniety, kod fix zostaje; przyszle zmiany juz normalnie. (2) Dodac 'vegan' do puli freestyle z lekko podbita szansa jak Snowy. Realizacja: 'vegan' dodany do WORDS (L39378, obok snowy/amper) + puli bonusowej; bonus 0.02→0.03 + losowanie 1/3 (snowy/amper/vegan) → snowy/amper zachowuja ~1% bonus, vegan dostaje tyle samo. Runtime: vegan 2.52% ~ snowy 2.38% ~ amper 2.27%, zwykle slowo 1.20% (2x mniej). 'vegan' NIE jest _adminTargets (brak ryzyka panelu). Patch note v849 zmieniony na notke o Veganie (AltGr silent). Commit e23f991.

### v2.1.850-851 — kolorowe emoji + wieksze wymogi piosenek koncertow (2026-06-12)
(1) **Patch notes cleanup (user)**: usunieta z notki Vegana wzmianka "zwiekszona szansa jak snowy/amper" (zostalo tylko "Vegan pojawia sie jako slowo we freestyle"); snowy/amper sprawdzone — brak takiej notki. **ZASADA: NIE pisac w patch notes ze snowy/amper/vegan maja zwiekszona szanse.**
(2) **v2.1.850 KOLOROWE EMOJI**: "czarna nieczytelna czcionka + dziwny odstep w patch notes" = MONOCHROMATYCZNE emoji (body font-family L13 bez color-emoji fonta -> czarne glify na ciemnym tle + odstep). Fix: dodane Segoe UI Emoji/Apple Color Emoji/Noto Color Emoji do stacku (lapia tylko emoji). Render patch notes byl strukturalnie czysty (darkCount=0, #dde) — problem tylko w foncie. LEKCJA: brak color-emoji fonta = czarne emoji na ciemnym tle.
(3) **v2.1.851 wieksze wymogi piosenek**: po info ze album rapera=5 trackow (releaseRaperAlbum L29128, kolejne +5), user wybral "mocniejsze": Mala 2->5, Klub 4->12, Arena 8->20, Coachella 15->30, Wembley 20->40, Megastar 25->50. Oferty (LEAD_TYPES songsReq) podniesione spojnie (club_local 5/club_warsaw 12/festival 18-30/radio 5-12/wedding 5/private 18; sponsor IG 0). Hint record_first prog 2->5. Runtime dbg_concert_thresholds_v851.mjs 12/12 PASS. Commity 9a2320d + a84edee.

### v2.1.853 — Twitter "[object Object]" w feedzie (2026-06-12)
User: "na twitterze wyskakuje dziwne [object Object]". Root cause: render feedu Twitter (renderPhoneApp twitter L41497) merge'uje user+generic tweety w obiekty: `[..._userTweets.map(t=>({...t,isUser:true})), ..._genericTweets.map(t=>({text:t,isUser:false}))]`. L41500 `const tweetText=t.isUser?t.text:t` zwracalo CALY obiekt `t` dla generic (zamiast `t.text`) -> `${tweetText}` = "[object Object]". Bug od v2.1.396 (Faza AP.2), widoczny po pierwszym tweecie gracza (generic uzupelnia feed do 5). Fix: `tweetText=t.text` (oba typy maja .text po merge). Runtime dbg_twitter_objobj_v853.mjs 6/6 PASS (generic+mixed bez object-Object, custom+generic widoczne, zero bledow). Commit a396b41. LEKCJA: ternary po merge tablic-obiektow MUSI wyciagac property w KAZDEJ galezi.

### v2.1.854 — "pieniadze za nic" po przerwie (clamp delty real-time) (2026-06-12)
Vegan: "wylogowalem sie i zalogowalem po ~30 min, mam +20 pare tys za nic". ROOT CAUSE (dlugo szukany objaw): gameLoop `loop()` L66005 `gameMin += ((nowT-lastClockReal)/1000)*4.0*tMul` BEZ clampa. Karta w tle = rAF zamrozony -> lastClockReal nieswiezy; po wznowieniu (powrot do karty / load save / logout->login) pierwsza klatka liczy ogromna delte (30 min = 1800s) -> gameMin += 7200 -> przeskok ~5 dni -> for(i<days) nextPeriod() = pasywny dochod (tantiemy/evolution/dealerzy/wynajem...) za niegrane dni = +20k. FIX 3-warstwowy: (1) CLAMP `const _realDeltaSec=Math.min(1.0,Math.max(0,(nowT-lastClockReal)/1000))` -> max 1 sek/klatka (62x normalnej 16ms; luka z tla nie dolicza czasu gry, nextPeriod nie odpala falszywie); (2) lastClockReal=Date.now() w startGame (L61842); (3) w loadGame (L60948). Istniejaca mitygacja byla TYLKO w togglePause (manualna pauza, L48384) - nie lapala load/tab-resume bo visibility handler pomija gdy inGame=false (menu). Runtime dbg_time_skip_v854.mjs 6/6 PASS (po 30-min nieswiezym zegarze: day/money bez skoku). Headless rAF nie tika -> zywa klatka nie obejrzana, clamp = trywialny matematyczny guard. Commit 5bc89fc. LEKCJA: rAF time-advance ZAWSZE clampowac delte (background-freeze->resume jump); clamp > poleganie na pause/visibility handlerach.
