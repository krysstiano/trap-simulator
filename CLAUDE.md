# Snowy Simulator — instrukcje dla Claude

## 🟢 NA START SESJI (2026-06-19, v2.3.33) — bugfix /loop ZAMKNIĘTY w trybie czuwania (gra zdrowa, judge zielony)
> 🚨🚨 **ZASADA #0 (NADRZĘDNA, ZAWSZE): DOWÓD, NIE SŁOWO.** „done"/„działa"/„zweryfikowane"/„na pewno done?"/„audyt OK" = WYNIK URUCHOMIONEGO testu (asercje+exit code; uruchamiam go automatycznie SAM co iterację ORAZ user może odpalić sam) + dokładna analiza — NIGDY moja deklaracja. Moje słowo zawodzi w obie strony (fałszywe „ok" i fałszywe „bug"). NIE ogłaszam „gotowe" prozą — pokazuję OUTPUT testu. NIE kończę dopóki ZIELONY. Pełna treść: sekcja ŻELAZNE REGUŁY #0 niżej + memory `feedback_proof_not_word_iron_rule`.

**STAN PO SESJI bugfix (CZYTAJ NAJPIERW memory `project_snowy_bugfix_loop` sekcja „PO /CLEAR — STAN KOŃCOWY"):** zbudowany SĘDZIA — **`node audit_rooms.mjs`** (260 przycisków × 19 pokoi + 21 flows runtime, exit 1 przy bugu) + **`node audit_user_questions.mjs`** (kradzieże/Instagram/Business District). Oba ZIELONE. **31 realnych bugów naprawionych** (v2.3.33, commity `3d3c550`→`91e698b`); najpłodniejsza klasa **crash-na-uszkodzonym-save DOMKNIĘTA** (root `window._sanitizePlayerLists()` w loadGame/togglePhone/renderPhoneApp/invInit czyści 25+ list gracza). Zgłoszenia usera (IG/dealer/stacje/business/kradzieże) zaadresowane i potwierdzone testem.
**JAK WZNOWIĆ:** nowe KONKRETNE zgłoszenie (obszar/PDF/„X nie działa" + który zapis/sekwencja) → odtwórz dokładnie w dbg Playwright, kliknij, asercja DZIAŁA, fix → dbg + dodaj FLOW do audit_rooms → Round 2 #18 → patch note Vegan (ZERO apostrofów/admin) → `git commit -F`. Ogólne „masa bugów" bez konkretu → odpal oba audyty, pokaż OUTPUT; zielone = NIE wymyślaj bugów (ZASADA #0), poproś o konkret. Lekcje: testuj wg cond (FALSE-POSITIVE=zepsuty test/bypass guardu) · korupcja REALISTYCZNA (null entry w listach do których gra robi push) · `a&&a.X` guard · reset stanu/flag VIP/cooldownów przed testem · kasa szumi od passive income (asercja na efekt nie exact-money) · `closeNPCDlg` nie wycieraj innerHTML. Harness: `_helper_runtime_template.mjs` setupPage; `changeRoom(id,wx,wy)` district spawns underground[1200,480]/coast[720,800]/business[720,60].
**DLACZEGO bugi przeciekły:** poprzednie audyty były STATYCZNE/grep + testowane TYLKO na świeżej nowej grze → **przegapiły bugi w treści ZABLOKOWANEJ za progresją** (Business District fame 300, lab trap-lvl 10, dealer, narkobiznes, kradzieże). **NOWA METODA (obowiązkowa): RUNTIME INTERACTION AUDIT w STANIE ODBLOKOWANYM** — Playwright harness → MAXED save (money 1e9, level 50, fame 999, trap.unlocked, trap.level 15, `_unlockedDistricts=[underground,coast,business]`) → wejdź do KAŻDEGO pokoju i KLIKNIJ KAŻDY `obj.act` (handleAct/_enterDistrictPlace) → throw/no-op/„w przygotowaniu"/ghost = REAL FINDING. **TYLKO kliknięty-i-działa = naprawiony (NIE grep, NIE „wierne").**
**ZGŁOSZENIA USERA (priorytet):** (1) Instagram nie działa; (2) narkobiznes PDF masa nie działa; (3) lab; (4) kradzieże okazjonalne; (5) dealer Underground „czasami"; (6) Business District „totalnie nic" + WSZYSTKIE inne bugi (sweep każdy pokój × przycisk × nowa-gra+wczytany-save).
**⚙️ DECYZJE 2026-06-19:** ⛔ **KONIEC deploy/Netlify co commit — cel docelowy = aplikacja EXE.** `deploy/index.html` NIE priorytet, single source = `index.html`. NIE przypominać o re-uploadzie/„stale build". (memory `feedback_no_deploy_netlify_spam`). Tej sesji już naprawione (Vegan): IG label-crash v2.3.29, mapa-w-budynkach v2.3.30, kolejka-komunikatów v2.3.31, sponsor-IG-followersReq v2.3.32. Bump v2.3.33+.
**KLUCZOWE LEKCJE (stosuj):** (1) agenci/grep na 74k linii = wysoki false-positive → **adversarialny grep PRZED każdą naprawą**. (2) **NIE testuj tylko nowej gry — treść gated wymaga MAXED save + klikania realnych przycisków** (to przegapiło Business District/lab/dealer). (3) `commit -F` (polskie znaki + apostrofy łamią `-m` ORAZ string JS w patch note → hook #2 blokuje — ZERO apostrofów). (4) Patch note Vegan ZERO-admin.
**AKTYWNY TEMAT (HISTORIA): realizacja PDF „Systemy i funkcjonalne moduły usprawniające narkobiznes (Trap Simulator)" w trybie /loop.** Komenda loop: `Pulpit/LOOP_NARKOBIZNES.txt`.
**CZYTAJ NA START (autorytatywne, w `E:\Snowy Simulator\`):** `DECYZJE_NARKOBIZNES.md` (decyzje usera — STOSUJ, NIE pytaj) + `PLAN_NARKOBIZNES_2026-06-17.md` (fazy TIER + statusy PENDING/DONE) + `PLAN_NARKOBIZNES_LEDGER.md` (line-item coverage PDF↔kod) + memory `project_snowy_narkobiznes_plan`. Ekstrakt PDF: `_narko_extract.txt`. Surowy triage 20-agentowy: `NARKO_TRIAGE.json` (zsyntezowany już do PLAN+LEDGER).
**✅ NARKOBIZNES 100% LITERALNE DONE (2026-06-18, v2.3.9) — FAZA A zamknięta.** TIER 1-12 + D1-D5 (systemy) + G1-G7 (braki mechaniczne) + Część IV #9 Blokada-kont + **H1-H8 literalne domknięcie nazwanych treści PDF** (H1 6-tierów-surowca/H2 fillery/H3 3-eventy-łapówkowe/H4 5-prawników/H5 3-ekipy/H6 multi-unlock+trigger/H7 wzorzec-zakupów/H8 kamper-cap-40%). Każdy z dbg PASS. **FINALNY MC PASS** (invariant trzyma, 0 spirali, launder EV<0, zero crash) + **MINI-AUDYT** (G+H na żywych ścieżkach). Ekstrakt PDF zweryfikowany kompletny (4 części). **✅✅✅ WERYFIKACJA 3 PDF + GŁĘBOKI AUDYT GRY: 100% DONE (2026-06-18, v2.3.10).** FAZA B zamknięta: PDF1 Błędy&wdrożenia + PDF2 Proponowane-zmiany + PDF3 Systemy-narkobiznes — wszystkie zweryfikowane (B0 ekstrakcja+kompletność / B1 agenci PDF↔kod / B2 synteza / B4 „na pewno done" 2 rundy + runtime smoke na wczytanym save / B5 głęboki audyt gry 2 rundy). **Zero potwierdzonych bugów/ghostów/exploitów** — wszystko z 3 PDF zaimplementowane i działa. ~20 agentowych „MISSING/crash" = WSZYSTKIE false-positive obalone adversarialnym grepem (#5); jedyna realna zmiana: save-hardening loadGame (v2.3.10, proaktywne #1). Pełny ślad: `VERIFY_3PDF_FINDINGS.md`. **LEKCJA: agenci Explore na pliku 74k linii → wysoki false-positive rate → grep-weryfikacja PRZED każdą „naprawą" obowiązkowa.** TEMAT (3 PDF + audyt) ZAMKNIĘTY — czekam na nowy temat usera.
**🛡️ PROTOKÓŁ JAKOŚCI (OBOWIĄZKOWY):** A=aktualizuj ledger po KAŻDEJ fazie · C=dbg test na WCZYTANYM starym save per TIER (zero crash + defaulty) ZANIM TIER=DONE · D=Monte Carlo całej ekonomii po TIER 4/7/koniec (trap ~15-18k, brak runaway) · E=ultracode workflow audytu CAŁEGO PDF na koniec przed „narkobiznes DONE". Każdy moduł hook do ≥2 innych (sprzężenia Cz III). Adaptacja nie duplikat.
**Wszystkie decyzje designerskie PODJĘTE** (skala ekonomii ÷10 MC-weryfikowana / legal↔illegal wyraźne #11 / treść kuratorowana ~4firmy-5-6NPC-3prawników / miękka porażka+łapówki-corruptionDep / WSZYSTKIE D1-D5 w zakresie) — szczegóły `DECYZJE_NARKOBIZNES.md`. **NIE pytać ponownie.**
**18 ŻELAZNYCH REGUŁ poniżej OBOWIĄZUJĄ** (save-safe #1 / syntax-check #2 / POST-grep #3 / analogi #6 / runtime-test #9 / #11-balans-gładko / atomic-commit #12 / Round 2 #18). Patch note Vegan ZERO apostrofów. `commit -F` + `cp -f index.html deploy/index.html`. Bump v2.1.987+. Każdą turę kończyć STATUSEM, nie pytaniem. Pauza tylko ręcznym sygnałem usera.

⛔ **NOWA REGUŁA (user „wielokrotnie", 2026-06-15):** ZERO info o panelu admin/dev ani dostępie (`?dev=1`, `M+M+M`, sekwencje) w tekstach dla graczy — zwłaszcza PATCH_NOTES. Po edycji patch notes → grep wycieków. Memory `feedback_no_admin_panel_info_for_players`. Komentarze kodu OK.

Dyscyplina per feature (gdy „leć dalej"/nowy plan): PRE-grep #13 → impl Edit/Node-fs (NIE PowerShell #4) → `dbg_*.mjs` runtime PASS (klik 18+ → newGameFromMenu → startGame) → Round 2 #18 → patch note Vegan `cats:['beta-tester','Vegan',typ]` ZERO-apostrofów → `git commit -F` → `cp -f index.html deploy/index.html` → memory. Wersje v969+. Pauza tylko ręcznym sygnałem usera.

## ⛔ ŻELAZNE REGUŁY — ABSOLUTNE, NIE WOLNO ŁAMAĆ

**Te reguły są ZAWSZE obowiązujące. Złamanie = błąd procesu. Pełne wersje + cytaty: `E:\Snowy Simulator\PLAN_NOC_2026-05-24.md` sekcja "ŻELAZNE REGUŁY" + memory `feedback_*_iron_rule.md`.**

0. **🚨 DOWÓD, NIE SŁOWO — ZASADA NADRZĘDNA NAD WSZYSTKIMI POZOSTAŁYMI (user 2026-06-19, dobitnie, „MASZ PAMIĘTAĆ O TYM ZAWSZE, W KAŻDEJ JEBANEJ SESJI").** Reguły, „na pewno done?", testy, analizy, weryfikacje, audyty to **NIE pytanie, na które odpowiadam swoim słowem** — to **realny, prawdziwy, rzetelny, dokładny, ZWERYFIKOWANY test + dokładna analiza**. Powód (z tej sesji): moje słowo zawodzi w OBIE strony — fałszywe „ok" (np. „można clear", a komenda była zepsuta; „nie rzuciło wyjątku" ≠ działa) ORAZ fałszywe „bug" (uznałem camper/plantation za no-op, a to działające przejścia). Dlatego ZAWSZE: **(a)** „done"/„naprawione"/„działa"/„zweryfikowane" = WYNIK URUCHOMIONEGO testu (asercje + exit code) + re-read, NIGDY deklaracja; **(b)** NIE ogłaszam „skończone" prozą — pokazuję **OUTPUT testu**; **(c)** test **uruchamiam automatycznie SAM w każdej iteracji / po każdej naprawie** (przez Bash; bramkuje pracę — czerwony = pracuję dalej) **ORAZ** jest **uruchamialny przez usera** (`node X.mjs`, exit 1 przy błędzie) — user weryfikuje SAM, bez ufania mi; **(d)** grep / „kod napisany" / „nie rzuciło" = NIE dowód — wymóg: zaobserwowany właściwy efekt runtime (panel z treścią / poprawny stan / kliknięcie zadziałało); **(e)** NIE kończę dopóki test ZIELONY — czerwony = pracuję DALEJ, DO SKUTKU (choćby tydzień); „brak czasu"/„wydaje się"/„powinno działać" NIE istnieją. Memory `feedback_proof_not_word_iron_rule`. **Ta zasada bije wszystkie inne — jeśli kiedykolwiek mam ochotę napisać „gotowe" bez zielonego testu, to znaczy że NIE jest gotowe.**
1. **NIE łamać save'ów graczy** — backward compat wymagany, jeśli nie da się → poinformować user PRZED commitem.
2. **ZAWSZE syntax check po każdym Edit** — extract JS + `node --check _syntax_check.js`. NIE twierdź "zrobione" bez weryfikacji. (Hook PostToolUse `hook_syntax_check.mjs` wymusza to mechanicznie.)
3. **POST-impl audit: grep WSZYSTKICH miejsc dla zmiany** — UI/opisy/storyline/hints/achievementy/SMS/dialogi/mapa/save keys/PATCH_NOTES/tutorial/cinematic taglines/telefon panele/NPC dialogi/smart hints. Niezgodne opisy = NIE commitować.
4. **NIE używać PowerShell na index.html** — UTF-8 broken na Polish locale, używać Edit/Write/Bash Node fs.
5. **NIE zgaduj — weryfikuj kod PRZED odpowiedzią** — każda liczba/formuła/czas/mechanika MUSI być Grep/Read przed napisaniem.
6. **DONE = grep KAŻDEJ analog feature** — apartment/car/outfit/NPC → grep analogi (np. mansion → apt_premium) i sprawdź 8-15 miejsc.
7. **NIE zamiataj problemów pod dywan** — ghost feature / UI obietnica → naprawić kod, NIE usuwać obietnicy. Dead code → AKTYWOWAĆ/ROZWIJAĆ, NIE usuwać.
8. **ZERO TOLERANCE na overlap** — PRZED dodaniem KAŻDEGO obiektu wizualnego sprawdź kolizję z DB_DECORATIVE + budynkami + ozdobami + drogą.
9. **Runtime test PRZED "naprawione"** — Playwright dbg_*.mjs lub page.evaluate() z asercją. Tylko gdy PASS → "naprawione".
10. **NIGDY DONE przy <100% PASS** — status NIEKOMPLETNE z raportem zamiast false "skończone".
11. **Soft cap/balans = brak wrażenia odbierania** — gładka pochodna (brak progu), monotonicznie rosnąca, asymptota (nie hard cap).
12. **Atomic commits per faza** — każda zmiana = osobny commit z opisem. Łatwy rollback.
13. **Plan ≠ stan kodu — grep PRZED implementacją feature z planu** — jeśli istnieje → BLOCKED + skip.
14. **PRE-impl: PRZED zmianą WARTOŚCI/POZYCJI/STAWKI/NAZWY — pełny audyt obszarów** — grep starej wartości + grep analogów + 14 obszarów + akcje per typ + test mentalny ścieżką gracza. (powtórzone przez usera 2026-05-16 i 2026-05-24 = sygnał krytyczności)
15. **NIGDY nie kończ pracy w autonomicznym trybie** — w /loop / autonomicznej sesji ABSOLUTNIE nie pisz "skończyłem", "podsumowuję" przed userem. Skończyłeś fazę → kolejna PENDING. Skończyłeś wszystkie → AUDIT MODE. Brak audytów do zrobienia → GŁĘBOKI AUDYT CAŁEJ GRY. Brak czasu nie istnieje. (user 2026-05-24, memory `feedback_autonomous_no_stopping`)
16. **AUTO-WERYFIKACJA po WSZYSTKICH fazach DONE** — gdy plan wykonany, BARDZO DOKŁADNIE sprawdź każdą fazę: czy zmiana faktycznie w kodzie, czy nie ma bugów, czy testy PASS, czy POST-impl audit zrobiony, czy nie pominąłeś nic. Znajdziesz problem → dopisz fazę naprawczą + napraw. NIE uznawaj planu za "ukończony" bez tej weryfikacji. (user 2026-05-24)
17. **GŁĘBOKIE AUDYTY CAŁEJ GRY** — gdy plan + auto-weryfikacja DONE, przejdź do deep audit WSZYSTKICH OBSZARÓW gry: szukaj bugów, ghost features, niespójności, exploitów, mylących UI, dead code do aktywacji, formuł które eksplodują. Naprawiaj. To NIE jest "filler" — to główne źródło jakości w autonomicznej pracy. (user 2026-05-24)
18. **"Na pewno done?" — 2-3 rundy Round 2 audytu PRZED declare DONE** — per każda faza w /loop, ZANIM mark DONE: Round 1 = impl + syntax + runtime + POST-impl grep. Round 2 = re-grep UI promise vs code reality, save compat defensive init, cap-ladder-ghost check, misleading text scan, analogi (#6, 8-15 miejsc). ZERO Round 2 findings → DONE. Inaczej fix + Round 3. (user 2026-05-25, memory `feedback_done_means_2_3_audit_rounds`. Test Faza 35 KASYNO: 3 rundy znalazły 5 ghost features bez których byłby false DONE.)

## 📝 BEST PRACTICES

- **Nowe wielkie systemy** (DUŻA) → defer do sesji z userem. MAŁA/ŚREDNIA OK.
- **Czas pracy** → używać złożoności MAŁA/ŚREDNIA/DUŻA, NIE estymować w godzinach.
- **Smoke/dbg_*.mjs/runtime testy** → autonomia z osądem (user 2026-05-24). WARTO przy: mechanice (formuła earn/cap/mnożnik/cooldown), state machine (multiday tour/quest/save format), refactor systemowym, PRE/POST-impl audit dla wartości. NIE warto: wizualne (kolor/font), tekst/opisy, refactor bez zmiany zachowania. Reguła #9 (Runtime test PRZED "naprawione") nadal obowiązuje gdy zmiana mechaniki.
- **BATCH MODE per turn** (user 2026-05-25): 2-5 zadań/turn dla MAŁYCH (text/font fix) + ScheduleWakeup delaySeconds=60s. Single per turn dla ŚREDNICH/DUŻYCH (mechanika/balans). Anti-context-bloat: save state explicit per zadanie.

## 🌙 Autonomiczna sesja /loop (AKTUALNY PRIORYTET: PLAN_DZIELNICE)

Jeśli pracuję w trybie /loop:
1. **PRIORYTET: `PLAN_DZIELNICE_2026-05-26.md`** (user 2026-05-27) — czytaj go na początku każdej iteracji (workflow krok 1). `PLAN_NOC_2026-05-24.md` = reference background (workflow + 27 zadań audit mode + Faza R2 procedura).
2. Następna faza = pierwsza ze statusem `PENDING` zgodnie z TIER order (TIER 1 P0 bugfixy → TIER 7 deep audits).
3. Pełny workflow + edge cases + Audit Mode w sekcji "Workflow per iteracja" PLAN_DZIELNICE (analog PLAN_NOC).
4. **NIGDY** nie zatrzymuj się "wait user" — jeśli blocker → skip + audit mode.
5. **NIE pytaj usera o decyzje designerskie** — wybieraj sam zgodnie z planem + memory feedback_*, zapisuj decyzje do PLAN_DZIELNICE sekcja "❓ Pytania do usera (rano)" (user 2026-05-27).
6. **Round 2 "na pewno done?" audyt obowiązkowy** per każda faza ZANIM mark DONE (reguła #18).

(Hook UserPromptSubmit `hook_load_plan_noc.mjs` auto-doładuje CLAUDE.md zawsze + PLAN_DZIELNICE/PLAN_NOC przy trigger words "kontynuuj"/"snowy"/"dzielnice"/"plan_dzielnice"/"plan_noc"/"wracamy". Mechaniczne wsparcie pamięci.)

## 🎮 Aktualny stan gry

- **Wersja**: **v2.1.807** (2026-06-05). ✅ PRE-LAUNCH READY (v773-781) + 🪴 REDESIGN UPRAWY FAZA 1/4 (v782-784 — Kamper baza startowa + UX uprawy (czas/leczenie/wzrost 2dni) + INTERAKTYWNE MINIGRY CANVAS uprawy (v785: przeciagasz nasiono/worek/konewke/nozyce + v786 woda/nawoz do 100% + v787 minigry suszenia(hang)/pakowania(pack) + v788 cook-zlewka + v789 reaktor-temp/kokaina/grzyby = CALA produkcja ma minigry canvas + v790 FAZA 2: Plantacja-pokoj 6-doniczek + v791 pakowanie=przeciaganie + topy + v792 naprawy trap: spam dealer/terytorium, heat/kontrola wyjasnione, telefon=podglad + v793 ulepszenia produkcji uprawa/lab + rozbudowane Anti-Raid + v794 audyt ulepszen: 10/10 efektow zweryfikowane runtime + v795 FAZA 4 pracownicy: ogrodnik auto-uprawa + chemik auto-gotowanie + v796 Trap->folder Kariera + v797 FAZA 3: nowe substancje lab-only LSD/Ecstasy + prekursor + tier wartosci + v798 jakosc partii + v799-802 batch trap/onboarding/samolocik/login-widget + v803 liczniki kasyna sesja+overall(20 gier) + v804 szafka zaopatrzenia pracownikow + v805 BALANS MC: aparatura nie dla automatu chemika -> max automat 18.7k=muzyka + v806 Lapka bardziej nieprzewidywalna + v807 deep audit: Ride the Bus dolaczony do rekordow/licznikow=21 gier, 2 stare testy usuniete; CALY BATCH 2026-06-05 UKONCZONY 5/5), plan `adna-z-tych-opcji-happy-crystal.md`, memory `project_snowy_drug_redesign_phases`): pełny audyt gotowości do publikacji (4 agenty + workflow wizualny 16 regionów dzień+noc) — kod/UI/tekst/balans/mechaniki/save/wizualny-mapy/sprite-NPC CZYSTE; jedyny realny finding w całej grze = naprawiony **P0 exploit stakingu krypto** (nieograniczony compound ~×98/rok → asymptota `h.stakeAccrued/1.5`, reguła #11, MC ×98→×3.78) + **REKORDY KASYNA przebudowane** (root „nie działa"=był global-only; teraz lokalny rekord ZAWSZE widoczny, metryka kwota/mnożnik, optymistyczny TOP1, wheel dostał baner) + gameMin persyst + drobne polish. **+ AUDYT MUZYKI v775** (user „czy wytwórnie/dystrybutorzy/minigry/miks-master-sekwencer mają sens"): dystrybutorzy (req-gated trade-off) + 6 minigier + miks/master/sekwencer = OK; wytwórnie sens ALE niespójne cięcie tantiem → **fix: global tnie teraz albumy** (`_labelRoyaltyCut` single source, było tylko major), single CELOWO bez cut (decyzja usera 2026-06-04). **+ DOAUDYT v776 koncerty/trasy/albumy/radio**: koncerty (hiperboliczny soft-cap 180k) + trasy (state machine 3 typy) + albumy = EXCELLENT/OK; radio — **naprawiony BUG podwójnej wypłaty tantiem** (tick earn + collect = 2× → teraz single-pay przy „Zbierz"). Radio `maxPlays` EGZEKWOWANE v777 (bity rotują z anteny, onAir budżet). Memory `project_snowy_music_audit_v745_751` (sekcje doaudyt). Commity `c964847`/`ba35001`. Memory `project_snowy_prelaunch_polish_v774` + `project_snowy_casino_records_v773`. Akcje usera: upload `index.html` na itch + klasyfikacja 18+ (opcjonalnie `SUPABASE_CASINO_RECORDS_CLEANUP.sql`). — Wcześniej **v2.1.772** ⛔ **KRYTYCZNE save-safety** (reguła #1): naprawiony ROOT CAUSE „znikających save" — boot robił `await _idbGet` (IndexedDB) bez try/catch+timeout → na `file://` open RZUCA/ZAWISA → `refreshContinueButton` nie renderował slotów → puste menu. Fix v771-772: **render slotów PRZED IndexedDB** + `_idb` timeout 2500ms + **recovery UI w menu** (backupy widoczne „🛟Odzyskaj") + główny-zapis-przed-backupami + NaN-guard slotu + koercja typów. Memory `project_snowy_save_loss_rootcause_2026_06_04`. Wcześniej tej sesji: **PRE-LAUNCH** age-gate 18+, anty-XSS UGC społeczności (escape cudzych nick/caption/komentarz), panel admina za `?dev=1`+anti-cheat, nota o danych, `DEPLOY.md`; **mobile touch** myjnia/zmywak; audyt kodu (XSS fallback/null-deref sklepy/lyric+bts/Trendsetter) + balans early-game ZDROWY; **Amper = beta-tester, atrybucje ZOSTAJĄ**. Sesja: memory `project_snowy_session_2026_06_04_ui_casino_v752_757`. — Wcześniej **v2.1.654** (2026-06-02: KASYNO — v640 SAMOLOCIK przebudowany + v642 SIDE-SCROLLER + v643 fizyka grawitacji + v644 (dźwięki/endless/zmienność) + v645 (mnożnik skok ✖️/÷2 🚀, cichszy silnik) + v646 (balony rzadziej+mocniej podbijają) (user: za podobny do Crash → lot nad wodą/lotniskowce, czysta loteria BEZ cashoutu, mnożniki/rakiety/podmuchy, canvas, RTP 95.97% Pareto cap250; wideo YT niedostępne → z opisu usera; memory `project_snowy_samolocik_redesign`), v641 HISTORIA gier (Crash wybuchy + Ruletka wypadnięcia, `G.casinoHistory`). Wcześniej dokończenie AN.8 narkotyki — v636 EFEKTY WIZUALNE per substancja (`drawDrugTripFX` 4 overlaye, hook loop()), v637 ONBOARDING (przelot temat „Dzielnice/Pies/Ekwipunek I" + Mechaniki wpis), v638 GRZYBY creativity bonus +10 jakości nagrań 24h (naprawa ghost flag `_mushroomFameDay`), v639 WITHDRAWAL WIZUALNIE (`drawWithdrawalFX` chorobliwy tint gdy addiction≥50+1dzień). Pozostały marginalny detal AN.8: weed „-1 fame przy spotkaniu z fanem" (nie obiecane w desc, nie ghost). Wcześniej ROZBUDOWA SYSTEMU PRZEDMIOTÓW — user „zrób 2.3.4 też" + „lecimy z opcjonalnym" = 6 faz. v630 sprzęt kariery (equip-stat `fame`→addFans, 🎤 mikrofon), v631 +6 akcesoriów+3 sloty (feet/neck/back), v632 +3 buffy czasowe (fans/xp/fame), v633 ekonomia materiałów (drop z koncertu+ulicy, rzadki 🔌 mikroczip, premium 🎛️ DJ konsola), v634 składniki z freestyle/breakdance (mikroczip koncert-only), v635 +2 premium tier-2 (💎 naszyjnik, ⌚ smartwatch Pro). Suita item-system 11/11 dbg PASS, Round 2 czysty per faza. Szczegóły: memory `project_snowy_item_system.md`. Wcześniej: PEŁNY SYSTEM PRZEDMIOTÓW v625-628, +2 receptury v629).
- **Plik**: `E:\Snowy Simulator\index.html` (~65 600 linii, ~4.9 MB, single-file HTML5 Canvas game, polski język).
- **System przedmiotów** (gdy user wraca do tematu): memory `project_snowy_item_system.md`. Itemy mają `kind` (valuable/equip/consumable/quest/material). Funkcje przy `openInventory`/`ITEM_CATALOG`. POZOSTAŁE rozszerzenia: ew. więcej receptur/bonus-typów.
- **Najnowsze sesje** (chronologicznie wstecz):
  - **2026-06-04 (cd., v2.1.773-774)** — PRE-LAUNCH AUDYT + przebudowa rekordów kasyna. **Rekordy**: root „dalej nie działa" = system był global-only (lokalny rekord nigdzie niewidoczny) → teraz **Twój rekord ZAWSZE widoczny**, metryka per typ (gry losowe=najwyższa wygrana / stołowe=łączna KWOTA, user 2026-06-04), optymistyczny TOP1, celebracja rekordu, **wheel dostał baner** (luka). **Audyt gotowości**: 4 agenty (martwe-UI/tekst/ekonomia/mechaniki-save = CZYSTE) + workflow wizualny 16 regionów (mapa czysta) + sprite-NPC code-verified (wszystkie skargi bledy.txt naprawione, wiele overlapów usunięto już 2026-05-12). **1 P0**: exploit stakingu krypto (asymptota, MC ×98→×3.78) + 5 polish (gameMin persyst/achievements guard/renderWIP/#hint/nick). Commit `c964847`. Memory `project_snowy_prelaunch_polish_v774`+`project_snowy_casino_records_v773`+`reference_bledy_txt_scratchpad`. **GRA GOTOWA DO PUBLIKACJI** (zostają akcje usera: itch upload + 18+). **PRZECZYTAJ gdy user wraca.**
  - **2026-06-04** (v2.1.752-772) — UI/kasyno/odds + REKORDY kasyna + **PRE-LAUNCH** (age-gate/anty-XSS UGC/admin za ?dev=1/DEPLOY) + mobile touch + audyt kodu/balansu + ⛔ **KRYTYCZNY save-loss root cause naprawiony** (IndexedDB boot). Memory `project_snowy_save_loss_rootcause_2026_06_04` (CZYTAJ NAJPIERW — reguła #1) + `project_snowy_session_2026_06_04_ui_casino_v752_757`. **Amper=beta-tester.** Stan: gra gotowa do publikacji web/itch (zostają akcje usera: upload `index.html`, klasyfikacja 18+; SUPABASE_WSZYSTKO.sql już uruchomiony przez usera). **PRZECZYTAJ gdy user wraca.**
  - **2026-06-01** (batch A–S, v2.1.610-618) — kasyno+ekwipunek+HUD+mobile+balans+gating+weryfikacja, 9 audytów czystych. Memory `project_snowy_session_2026_06_01_batch_AS.md`.
  - **2026-05-27** (PLANNING) — przygotowanie /loop dla PLAN_DZIELNICE, memory `project_snowy_session_2026_05_27_dyrektywy_dzielnice.md`.
  - **2026-05-25 iter385-425** — 41 iter, 43 commits, 28 system areas audit-hardened, 70/70 regression PASS. Memory `project_snowy_session_iter385_425.md`.
  - **2026-05-25 iter288-367** — autonomic 80 iter, Faza 35 KASYNO BALANS, Round 2 audit dyscyplina. Memory `project_snowy_session_2026_05_25_autonomous.md`.
  - **2026-05-24** — v2.1.232 Krok 1 endgame venues + balans + soft cap. Memory `project_snowy_session_2026_05_24.md`.
- **Tile size**: T=48px, świat ~4320×3600px (PRE-expansion; PLAN_DZIELNICE rozszerzy do 6480×5184).
- **Test runtime**: 70/70 PASS `dbg_concert_balance_v232.mjs` (baseline od v2.1.232). Suite Round 2 audit iter385-425 — verified clean dla 28 systemów.

## 🔍 Jak znaleźć kod w pliku

Plik rośnie — **NIE polegaj na statycznych numerach linii**. Używaj wzorca **`grep "═══ NAZWA"`** żeby znaleźć aktualną pozycję sekcji:

```
Grep: "═══ ACHIEVEMENTS"        → osiągnięcia
Grep: "═══ RANDOM_EVENTS"       → losowe zdarzenia
Grep: "═══ QUEST_POOL"          → misje dzienne
Grep: "═══ NPC_DATA"            → dane dialogów NPC
Grep: "═══ FOOD_CATALOG"        → jedzenie + ceny
Grep: "═══ RADIO_TIERS"         → radio
Grep: "═══ CAR_COLORS"          → kolory/kształty aut
Grep: "═══ drawCityLife"        → ruch NPC i aut
Grep: "═══ drawCityDecorations" → jezdnie, pasy, latarnie
Grep: "═══ SKILL_DEF"           → definicje umiejętności
```

Plus przy konkretnych zmianach: `Grep "<funkcja/zmienna>"` żeby znaleźć aktualną definicję.

## 📁 Pliki planu — co używać, co ignorować

**Aktualne (używaj)**:
- `CLAUDE.md` (ten plik) — 18 żelaznych reguł + skrót
- **`PLAN_DZIELNICE_2026-05-26.md`** — AKTUALNY PRIORYTET: 6 faz dzielnic (A-F) + 23 fazy bugfix/UX (G-AE) z user feedback 2026-05-27 + 7-tier order
- `PLAN_NOC_2026-05-24.md` — reference background: 35 faz historycznych + 27 zadań audit mode + Faza R2 procedura Round 2 audit
- `PLAN_8_KROKOW.md` — 8 kroków rozwoju (wszystkie DONE w v2.1.232)
- Memory `project_snowy_session_iter385_425.md` — najnowsza sesja autonomous
- Memory `project_snowy_session_2026_05_27_dyrektywy_dzielnice.md` — dyrektywy aktualnego /loop
- Memory `feedback_done_means_2_3_audit_rounds.md` — reguła #18 źródło
- Memory `MEMORY.md` (auto-loaded) — indeks wszystkich memory

**LEGACY (NIE używaj jako referencja stanu)**:
- `NOTATKI.md`, `PLAN_SESJA.md`, `PLAN_44_PUNKTY.md`, `PLAN_DECISIONS.md`, `PLAN_GLOSSARY.md`, `PLAN_INDEX.md` — historyczne
- `PLAN_AUTONOMOUS_2026-05-13.md`, `PLAN_PROGRESS_2026-05-12.md`, `PLAN_SESJA_2026-05-15.md`, `PLAN_SESJA_2026-05-17_PENDING.md`, `PLAN_MULTIPLAYER.md`, `PLAN_INSPIRACJE_2026-05-25.md` — historyczne plany
- `AUDYT_*.md`, `RAPORT_*.md`, `CHANGES_AUDYT.md`, `ANALIZA_*.md`, `KATEGORIE_REGULY.md`, `AMBIVALENCJE.md`, `ZESTAWIENIE_PREDKOSC.md` — historyczne audity/raporty
- `.backup_v2.1.*` / `.backup_NOC_faza_*` — historyczne backupy (zachowane jako safety)
- `SUPABASE_*.sql`, `_*.js` (scratch analizy), `_extracted.js` — robocze pliki

**Stan kodu** zawsze wcześniej WERYFIKUJ przez `Grep`/`Read`, NIE polegaj na żadnym z PLAN_*.md jako autorytatywnym źródle (reguła żelazna #13).

## 🔧 Hooks aktywne (Snowy Simulator)

- **PostToolUse Edit/Write** → `hook_syntax_check.mjs` (BLOKADA jeśli broken JS po patch index.html — reguła #2)
- **PreToolUse Edit/Write** → `hook_pre_edit_reminder.mjs` (przypomnienie #3 POST-impl/#6 analog/#13 grep first/#14 PRE-impl)
- **PreToolUse Bash (git commit)** → `hook_pre_commit_check.mjs` (checklist 8 punktów)
- **UserPromptSubmit** → `hook_load_plan_noc.mjs` (auto-load CLAUDE.md zawsze; PLAN_DZIELNICE/PLAN_NOC przy trigger words)
